import { ApiService } from "./api.service";
import { WooCommerceService } from "./woo.service";

type CreateUserReservationType = {
    user_id: string;
    
}

class ReservationService extends WooCommerceService {
    async createUserReservation({
        
    }) {
        
    }
}